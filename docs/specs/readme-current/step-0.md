the README's screenshot is out of date: it still shows the old keypad (7 8 9 on top) and no
New button

- amend the readme capability to add that the README should reflect the current state of
  the app
- update the screenshot so it does
