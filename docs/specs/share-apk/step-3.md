---
idea: share-apk
chain: trunk
step: 3
refines: step-2.md
---

# Step 3: apply the signing-key answer; every item to land is now settled

## This step

Applies the answer to `q-signing-key`, the chain's last open question. The step settles
`REQ-ci/debug-key-secret`, `CAP-ci/release-on-push` and `CAP-root/debug-key`, the three items
it blocked. With them, all ten items and six files are settled, and they land in the next
run. As the answer asks, this run made the keystore with `keytool` in the build image, with
Android's debug defaults, outside the repository, and set the `DEBUG_KEYSTORE` secret from
it with `gh secret set`. The host's permission check refused the first attempt, and the
retry passed once the user authorized it. `REQ-ci/debug-key-secret` lands in the next run,
with its `gh secret list` demonstration. `gh repo view` already shows the repository public. Nothing
lands in this step: the answer was the user's choice, not a standing authorization.
`status: refining → refining`, `remaining: 1q + 0r + 10i + 6f → 0q + 0r + 10i + 6f`.

## Changes

- **`q-signing-key`** — answered
  - **was:** "Who creates the fixed debug keystore and its secret, and is a copy kept?"
    (options (a) the next run creates it with `keytool` inside the build image, with
    Android's debug defaults, sets the `DEBUG_KEYSTORE` secret with `gh secret set`, and
    leaves the keystore in the scratchpad for you to keep outside the repository; (b) you
    create it and set the secret yourself; (c) as (a), but no copy is kept; unblocks
    `REQ-ci/debug-key-secret`, `CAP-ci/release-on-push`, `CAP-root/debug-key`)
  - **now:** (a): "Next run makes it, you keep a copy (Recommended)" (2026-09-23, in the
    conversation). It produces the settled status of `REQ-ci/debug-key-secret` below. It
    leaves `CAP-ci/release-on-push` and `CAP-root/debug-key` unchanged in text; no open
    question lists them now, so both are settled.
  - **verdict:** discharged
- **`REQ-ci/debug-key-secret`** — refined (its status line)
  - **was:** "**status:** planned"
  - **now:** "**status:** planned; settled by `q-signing-key`: the keystore is made with
    `keytool` in the build image, with Android's debug defaults; the secret is set from it
    with `gh secret set`, and the user keeps a copy outside the repository, so the secret
    can be restored if it is lost; it lands once `gh secret list` names `DEBUG_KEYSTORE`"
  - **verdict:** discharged
- **Open Questions section** — now says "No open questions."
  - **verdict:** discharged

## Amendments

None. Answer (a) is applied as given: the run made the keystore and set the secret. The
user keeps the copy.

## Demonstrations

None: nothing lands. For the record, the keystore was generated by running `keytool
-genkeypair` with store and key password `android`, alias `androiddebugkey`, RSA 2048, and
10000 days of validity, in `mental-arithmetic-build`, into the session's scratch directory.
It was then set with `base64 -w0 … | gh secret set DEBUG_KEYSTORE`. `gh secret list`
shows `DEBUG_KEYSTORE 2026-09-23T09:42:34Z`, and `gh repo view` shows
`"visibility":"PUBLIC"`.

## Reviews

None: nothing in the tree changed.

## Proof

- **re-proof of step-2** — skipped: only `q-signing-key`'s `answer:` line changed since
  step-2's commit
- **rounds** — two
  - round 1: `answers-applied` and `amendments` failed. The step had handed setting the
    secret to the user, which is option (b)'s path, and recorded no amendment. Fixed by
    keeping (a): the user authorized a retry, the run set the secret, and the step says so.
- **matched** — discharged; three plan changes, three Changes entries, no spec diff
- **coverage** — discharged; 17 items and 6 file lines untouched, and no untouched item
  is made false
- **no-widening** — discharged; no claim text changed
- **no-narrowing** — discharged
- **answers-applied** — discharged; (a) is applied in full: the run made the keystore and
  set the secret, and the user keeps the copy
- **justified** — discharged; no new items
- **consistent** — discharged; the repository shows public, and the status line fits
  `a-key-as-default-keystore`
- **settled** — n/a; nothing landed
- **landed**, **demonstrated**, **tree-kept** — n/a; no tree file changed
- **files** — discharged; file lines unchanged, and no other chain in flight lists them
- **progress** — discharged; closes the last open question (1q → 0q)
- **amendments** — discharged; none
- **terminal** — n/a
- **partition**, **independent**, **shared**, **cross-chain changes** — n/a
